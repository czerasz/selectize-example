# [selectize.js](http://selectize.github.io/selectize.js/) Heroses Usage Example

Install required packages and libraries:

   npm install
   bower install

Start client:

    gulp serve

Start server (API):

   npm start
